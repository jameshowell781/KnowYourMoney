@model IEnumerable<KnowYourMoney.Models.tblDeposit>

@{
    ViewBag.Title = "demo_user Deposits";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>demo_user Deposits</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DepositAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblAccountInfo.UserLogIn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DepositAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblAccountInfo.UserLogIn)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DepositID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DepositID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DepositID })
        </td>
    </tr>
}

</table>
<table class="table">
    <tr>
        <th>
            Total Deposits
        </th>
        <th style="padding-right:90px;">
            @ViewData["deposit_total"]
        </th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
    </tr>
</table>